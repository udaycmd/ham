[package]
name = "ham"
version = "0.0.1"
edition = "2024"

[dependencies]
rand = "0.10.1"

[dev-dependencies]
criterion = { version = "0.8.2", features = ["html_reports"] }

[[bench]]
name = "lexer_bench"
harness = false
